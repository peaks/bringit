import 'package:flutter/material.dart';

class RepositoryStatus extends StatelessWidget {
  const RepositoryStatus();
  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text(
      'Repository Status',
      style: TextStyle(fontSize: 16),
    ));
  }
}
