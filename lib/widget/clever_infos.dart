import 'package:flutter/material.dart';

class CleverInfos extends StatelessWidget {
  const CleverInfos();
  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text(
      'Clever Infos',
      style: TextStyle(fontSize: 16),
    ));
  }
}
