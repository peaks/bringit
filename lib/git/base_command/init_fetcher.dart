/* import 'package:git_ihm/git/base_command/terminal_command.dart';

class InitFetcher {
  Future<String> fetch(String directoryPath) async {
    // don't send back ignored files
    final TerminalCommand command = TerminalCommand('git', <String>['init']);

    return command.run(directoryPath);
  }
}
 */